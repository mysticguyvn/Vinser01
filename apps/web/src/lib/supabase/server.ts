/**
 * Server-side Supabase client for use in Server Components,
 * Server Actions, and Route Handlers.
 * Thin wrapper re-exporting from @vinser/core.
 */
export { createClient } from "@vinser/core/supabase/server";
